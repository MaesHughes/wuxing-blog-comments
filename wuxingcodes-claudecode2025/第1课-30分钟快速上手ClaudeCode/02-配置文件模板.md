# ClaudeCode配置文件模板

## 基础配置文件

配置文件位置：[`configs/config.json`](configs/config.json)

### 功能说明
- 包含完整的ClaudeCode配置选项
- 适用于大多数开发场景
- 包含权限管理、钩子设置、插件配置等

### 使用方法
```bash
# 复制到Claude配置目录
cp configs/config.json ~/.claude/

# 或根据需要修改后使用
```

### 完整配置内容
<details>
<summary>点击查看 config.json 完整内容</summary>

```json
{
  "model": "claude-3-5-sonnet-20241022",
  "maxTokens": 4096,
  "temperature": 0.1,
  "topP": 0.95,
  "theme": "dark",
  "autoSave": true,
  "keybindings": "vscode",
  "fontSize": 14,
  "fontFamily": "JetBrains Mono",
  "lineNumbers": true,
  "wordWrap": true,
  "statusLine": {
    "enabled": true,
    "format": "{{model}} | {{tokens}}"
  },
  "permissions": {
    "allowedTools": [
      "Read(**/*)",
      "Edit(**/*.{js,ts,jsx,tsx,py,java,cpp,c,h,go,rs,php,rb,swift,kt,scala})",
      "Bash(git:*)",
      "Bash(npm:*)",
      "Bash(node:*)",
      "Bash(python:*)",
      "Bash(pip:*)",
      "Bash(mvn:*)",
      "Bash(gradle:*)",
      "Bash(cargo:*)",
      "Bash(docker:*)",
      "Bash(make:*)",
      "Bash(gcc:*)",
      "Bash(g++:*)",
      "Bash(javac:*)",
      "Bash(java:*)",
      "Bash(python3:*)"
    ],
    "deniedTools": [
      "Edit(/etc/*)",
      "Edit(/usr/local/etc/*)",
      "Edit(/System/*)",
      "Bash(rm -rf:*)",
      "Bash(sudo rm -rf:*)",
      "Bash(chmod 777:*)",
      "Bash(dd:*)",
      "Bash(format:*)"
    ],
    "permissionMode": "acceptEdits"
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Executing command: {{command}}'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'File edited: {{file}}'"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "type": "command",
        "command": "echo 'ClaudeCode session started'"
      }
    ]
  },
  "plugins": {
    "enabled": ["git", "docker"],
    "autoLoad": true,
    "config": {
      "git": {
        "autoCommit": false,
        "autoPush": false,
        "commitTemplate": "{message}\n\nGenerated by ClaudeCode"
      }
    }
  },
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."]
    }
  },
  "sandbox": {
    "allowUnsandboxedCommands": false
  },
  "logging": {
    "level": "info",
    "file": "~/.claude/logs/claude.log",
    "maxSize": "10MB",
    "maxFiles": 5
  },
  "cache": {
    "enabled": true,
    "maxSize": "1GB",
    "ttl": "24h"
  }
}
```

</details>

## 简化配置文件

配置文件位置：[`configs/config-simple.json`](configs/config-simple.json)

### 功能说明
- 最小化配置，仅包含必要选项
- 适合初学者快速上手
- 可作为自定义配置的起点

### 使用方法
```bash
# 复制并重命名
cp configs/config-simple.json ~/.claude/config.json
```

```json
{
  "model": "claude-3-5-sonnet-20241022",
  "theme": "dark",
  "autoSave": true
}
```

## 性能优化配置

配置文件位置：[`configs/config-performance.json`](configs/config-performance.json)

### 功能说明
- 优化响应速度和资源使用
- 增大缓存和上下文窗口
- 启用并行处理

### 使用方法
```bash
# 适用于性能敏感的场景
cp configs/config-performance.json ~/.claude/config.json
```

<details>
<summary>点击查看完整配置</summary>

```json
{
  "model": "claude-3-5-sonnet-20241022",
  "maxTokens": 8192,
  "temperature": 0,
  "cache": {
    "enabled": true,
    "maxSize": "2GB",
    "ttl": "48h"
  },
  "compression": {
    "enabled": true,
    "threshold": 0.8
  },
  "parallel": {
    "enabled": true,
    "maxConcurrent": 3
  },
  "performance": {
    "timeout": 60000,
    "retryAttempts": 3,
    "retryDelay": 1000
  }
}
```

</details>

## 安全配置

配置文件位置：[`configs/config-secure.json`](configs/config-secure.json)

### 功能说明
- 严格的权限控制
- 仅允许特定目录和操作
- 启用密钥扫描和域名白名单

### 使用方法
```bash
# 适用于处理敏感数据的项目
cp configs/config-secure.json ~/.claude/config.json
```

<details>
<summary>点击查看完整配置</summary>

```json
{
  "model": "claude-3-5-sonnet-20241022",
  "permissions": {
    "allowedTools": [
      "Read(./src/**/*)",
      "Edit(./src/**/*.{js,ts,jsx,tsx})",
      "Bash(git:status)",
      "Bash(git:add)",
      "Bash(npm:test)",
      "Bash(npm:lint)"
    ],
    "deniedTools": [
      "Edit(**/config/**/*)",
      "Edit(**/.env*)",
      "Edit(**/*key*)",
      "Bash(rm:*)",
      "Bash(sudo:*)"
    ],
    "permissionMode": "strict"
  },
  "security": {
    "scanSecrets": true,
    "blockSecretPatterns": [
      "password",
      "secret",
      "token",
      "key",
      "credential",
      "api_key",
      "apikey"
    ],
    "allowedDomains": [
      "api.anthropic.com",
      "github.com",
      "npmjs.org",
      "pypi.org",
      "registry.npmjs.org"
    ]
  },
  "sandbox": {
    "allowUnsandboxedCommands": false,
    "allowedPaths": ["./src", "./tests"],
    "blockSystemPaths": true
  }
}
```

</details>

## 前端开发配置

配置文件位置：[`configs/config-frontend.json`](configs/config-frontend.json)

### 功能说明
- 集成ESLint、Prettier、Jest
- 自动格式化和代码检查
- React组件模板

### 使用方法
```bash
# 适用于前端项目
cp configs/config-frontend.json ~/.claude/config.json
```

```json
{
  "model": "claude-3-5-sonnet-20241022",
  "plugins": {
    "enabled": ["git", "eslint", "prettier", "jest"],
    "autoLoad": true
  },
  "hooks": {
    "PostEdit": [
      {
        "matcher": "**/*.{js,jsx,ts,tsx}",
        "hooks": [
          {
            "type": "command",
            "command": "npx eslint --fix {{file}}"
          },
          {
            "type": "command",
            "command": "npx prettier --write {{file}}"
          }
        ]
      }
    ]
  },
  "templates": {
    "reactComponent": "src/components/{{name}}/{{name}}.tsx",
    "styles": "src/components/{{name}}/{{name}}.module.css",
    "test": "src/components/{{name}}/{{name}}.test.tsx"
  }
}
```

## 后端开发配置

配置文件位置：[`configs/config-backend.json`](configs/config-backend.json)

### 功能说明
- 集成Python工具链（pytest、black、mypy）
- 自动代码格式化
- PostgreSQL MCP服务器支持

### 使用方法
```bash
# 适用于Python后端项目
cp configs/config-backend.json ~/.claude/config.json
```

```json
{
  "model": "claude-3-5-sonnet-20241022",
  "plugins": {
    "enabled": ["git", "pytest", "black", "mypy"],
    "autoLoad": true
  },
  "hooks": {
    "PostEdit": [
      {
        "matcher": "**/*.py",
        "hooks": [
          {
            "type": "command",
            "command": "python -m black {{file}}"
          },
          {
            "type": "command",
            "command": "python -m mypy {{file}}"
          }
        ]
      }
    ]
  },
  "mcpServers": {
    "database": {
      "command": "npx",
      "args": ["-y", "@anthropic-ai/mcp-server-postgres", "postgresql://localhost/mydb"]
    }
  }
}
```

## 团队协作配置

配置文件位置：[`configs/config-team.json`](configs/config-team.json)

### 功能说明
- 共享配置仓库
- 统一的commit和PR模板
- 自动代码审查

### 使用方法
```bash
# 适用于团队项目
cp configs/config-team.json ~/.claude/config.json
```

```json
{
  "model": "claude-3-5-sonnet-20241022",
  "team": {
    "sharedConfig": true,
    "configRepo": "https://github.com/company/claude-config",
    "syncInterval": "1h"
  },
  "git": {
    "autoCommit": false,
    "commitTemplate": "{type}: {message}\n\nCo-authored-by: Claude <noreply@anthropic.com>",
    "branchNaming": "feature/{{ticket}}-{{description}}",
    "prTemplate": "## Description\n\n## Changes\n\n## Testing\n\n"
  },
  "codeReview": {
    "autoReview": true,
    "rules": ["security", "performance", "best-practices", "style"],
    "autoFix": ["style", "best-practices"]
  },
  "companyAnnouncements": {
    "enabled": true,
    "message": "Welcome to Company's ClaudeCode setup! Check our internal docs for guidelines."
  }
}
```

## 环境变量配置

### 环境变量模板

配置文件位置：[`configs/.env.example`](configs/.env.example)

### 使用方法
```bash
# 复制模板并填写实际值
cp configs/.env.example .env

# 编辑.env文件，填入您的实际配置
```

```bash
# API配置
ANTHROPIC_API_KEY=sk-ant-xxx

# 模型配置
ANTHROPIC_DEFAULT_MODEL=claude-3-5-sonnet-20241022

# 调试配置
CLAUDE_DEBUG=0
CLAUDE_LOG_LEVEL=info

# 性能配置
CLAUDE_CACHE_ENABLED=1
CLAUDE_CACHE_SIZE=1GB

# 代理配置（如需要）
# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=http://proxy.company.com:8080
# NO_PROXY=localhost,127.0.0.1

# Git配置
GIT_AUTHOR_NAME=Your Name
GIT_AUTHOR_EMAIL=your.email@company.com
```

### VS Code设置

配置文件位置：[`configs/vscode-settings.json`](configs/vscode-settings.json)

### 使用方法
```bash
# 复制到VS Code设置目录
mkdir -p .vscode
cp configs/vscode-settings.json .vscode/settings.json
```

```json
{
  "claudecode.model": "claude-3-5-sonnet-20241022",
  "claudecode.autoSave": true,
  "claudecode.theme": "dark",
  "claudecode.fontSize": 14,
  "claudecode.keybindings": "vscode",
  "claudecode.statusLine.enabled": true,
  "claudecode.integrated": true,
  "files.autoSave": "afterDelay",
  "files.autoSaveDelay": 1000,
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  }
}
```

## 环境变量设置脚本

### Windows批处理

脚本位置：[`scripts/set-env.bat`](scripts/set-env.bat)

```batch
@echo off
echo 设置ClaudeCode环境变量...

REM API密钥
setx ANTHROPIC_API_KEY "sk-ant-xxx"

REM 默认模型
setx ANTHROPIC_DEFAULT_MODEL "claude-3-5-sonnet-20241022"

REM 缓存配置
setx CLAUDE_CACHE_ENABLED "1"
setx CLAUDE_CACHE_SIZE "1GB"

REM 日志级别
setx CLAUDE_LOG_LEVEL "info"

echo 环境变量设置完成！
echo 请重启命令行窗口以使更改生效。
```

### macOS/Linux Shell脚本

脚本位置：[`scripts/setup-env.sh`](scripts/setup-env.sh)

```bash
#!/bin/bash

echo "设置ClaudeCode环境变量..."

# 创建环境变量文件
cat > ~/.claude-env << 'EOF'
# ClaudeCode环境变量
export ANTHROPIC_API_KEY="sk-ant-xxx"
export ANTHROPIC_DEFAULT_MODEL="claude-3-5-sonnet-20241022"
export CLAUDE_CACHE_ENABLED="1"
export CLAUDE_CACHE_SIZE="1GB"
export CLAUDE_LOG_LEVEL="info"
EOF

# 添加到shell配置文件
if [ -f ~/.zshrc ]; then
    echo 'source ~/.claude-env' >> ~/.zshrc
    echo "已添加到 ~/.zshrc"
elif [ -f ~/.bashrc ]; then
    echo 'source ~/.claude-env' >> ~/.bashrc
    echo "已添加到 ~/.bashrc"
fi

# 立即生效
source ~/.claude-env

echo "环境变量设置完成！"
```